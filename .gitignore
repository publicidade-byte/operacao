node_modules
dist
.env
.env.local
.env.*.local
*.log
.DS_Store
.vscode
.claude
supabase/.temp
supabase/.branches

# Dados operacionais reais — calendário de operações e nomes de diretores.
# Ficam só no banco (Supabase) e em backup local fora do repositório.
supabase/seed.sql
data/

# Planilhas e exportações de trabalho — mesmo motivo do seed.sql: o calendário
# de operações é dado da empresa e o repositório é público.
*.xlsx
notion-*.csv
notion-*.md
painel-operacao.html
registro-automatico.gs
supabase/importar-*.sql
