# Copie para .env e preencha com os valores do seu projeto Supabase.
# Estas duas chaves são PÚBLICAS por design (protegidas por RLS).
# Nunca coloque a service_role key aqui — ela vive só nos secrets das Edge Functions.

VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...
